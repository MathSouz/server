const { user } = require("./user")
const { post } = require("./post")
const { comment } = require("./comment")
const { report } = require("./report")
const { token } = require("./token")

module.exports = { user, post, comment, report, token }
